[package]
name = "integration-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
common = { path = "../common", features = ["user"] }
offense = { path = "../offense" }
defense = { path = "../defense" }
